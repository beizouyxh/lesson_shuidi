<block wx:for="{{products}}">
    <van-card 
    price="{{Math.round(item.price)}}"
    desc="{{item.color}}"
    title="{{item.title}}"
    thumb="{{item.image}}"
    data-id="{{item._id}}"
    >
</van-card>
</block>

